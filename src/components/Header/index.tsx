import React from "react";

const Header = () => {
  return (
    <div className="flex items-center fixed top-0.5 w-screen">
      {/* Logo */}
      {/* menu */}
      {/* actions */}
      Header
    </div>
  );
};

export default Header;
